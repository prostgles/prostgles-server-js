{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc-watch --onSuccess \"node index.js\"",
    "dev": "NOSTOP=true npm start",
    "build": "rm -rf ./node_modules/* && npm i && npm run test-server && npm run test-client",
    "test-server": "tsc && TEST_TYPE=server node index.js",
    "test-client": "tsc && TEST_TYPE=client node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.19",
    "@types/socket.io": "^2.1.12",
    "@types/socket.io-client": "^1.4.34",
    "express": "^4.17.1",
    "prostgles-server": "file:../..",
    "socket.io": "^3.0.4"
  }
}
